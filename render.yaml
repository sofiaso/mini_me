services:
  - type: web
    name: rag-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn rag.main:app --host 0.0.0.0 --port 10000
    plan: free
    autoDeploy: true
    python_version: "3.10"